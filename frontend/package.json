{
  "name": "meander-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Meander \u2014 time-budgeted routing for greenery and real accessibility.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "dev:mock": "VITE_MOCK_API=1 vite",
    "build": "npm run check:palette && npm run check:permalink && npm run check:offline && vite build",
    "preview": "vite preview",
    "check:palette": "node scripts/check-palette.mjs",
    "check:permalink": "node scripts/check-permalink.mjs",
    "check:offline": "node scripts/check-offline.mjs",
    "icons": "node scripts/make-icons.mjs"
  },
  "dependencies": {
    "maplibre-gl": "^5.6.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "axe-core": "^4.12.1",
    "vite": "^6.0.7"
  }
}
