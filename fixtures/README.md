# Recorded upstream responses.
#
# Every file here is written by backend/fixtures.py. Secrets are redacted before
# a fixture is written, and API keys are excluded from the request signature so
# fixtures stay portable between contributors.
#
# Files carrying "_meander_provenance": "synthetic" were hand-built, not
# recorded. Any route derived from one is labelled scoring_method:"placeholder"
# in the API response.
